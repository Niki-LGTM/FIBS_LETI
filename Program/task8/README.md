# ИДЗ-8. Веб-приложение FastAPI: Дисциплины и запись студентов (N:M)

**Выполнил:** []  
**Вариант:** FastAPI + Jinja2 + pickle-хранилище  
**Оценка:** 10/10

## Выполненные пункты задания

| Пункт ТЗ                                                                  | Статус   | Реализация                                   |
|---------------------------------------------------------------------------|----------|-----------------------------------------------|
| 1.1–1.2 Класс Discipline с кодом, названием и методом добавления студента | Готово   | `app/core/models.py`                          |
| 2. Класс для связи «многие-ко-многим» студент ↔ дисциплина                | Готово   | `Enrollment` в `app/core/models.py`           |
| 3. Страница списка дисциплин (/disciplines)                               | Готово   | `disciplines_list.html`                       |
| 3.1 Детальная страница дисциплины (/disciplines/{code})                   | Готово   | `discipline_detail.html`                      |
| 3 3.1.1 Добавление студента на дисциплину (select)                        | Готово   | `/disciplines/{code}/add`                     |
| 3.1.2 Список студентов на дисциплине                                      | Готово   | `/disciplines/{code}/students`                |
| 3.2 Страница добавления дисциплины                                        | Готово   | `/disciplines/add`                            |
| 4. Хранение в отдельных бинарных файлах                                   | Готово   | `data/groups.pkl`, `students.pkl`, `disciplines.pkl`,  `enrollments.pkl` |
| 5. Отдельные кнопки сохранения в подвале                                 | Готово   | 4 кнопки в `base.html`, API `/save/*`         |
| 6. Все страницы через Jinja2-шаблоны с наследованием                     | Готово   | `base.html` + дочерние шаблоны                |

## Структура проекта (чистая архитектура)

task8/
├── main.py                     → точка входа
├── app/
│   ├── core/
│   │   ├── models.py           → Student, Discipline, Enrollment
│   │   └── repositories.py     → работа с pickle
│   ├── api/
│   │   └── routes.py           → все маршруты
│   └── presentation/
│       └── templates/          → все HTML-шаблоны
├── data/                       → создаётся автоматически
└── README.md                   → этот файл

## Как запустить

1. Установи зависимости:
   bash
   pip install fastapi uvicorn jinja2

2. В консоли прописать:
    cd "путь до папки где это все находится"
   
3. Команда для запуска
    uvicorn main:app --reload

    
